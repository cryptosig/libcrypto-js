(function(root, factory) {
  if (typeof exports === 'object' && typeof exports.nodeName !== 'string') {
    /* global require */
    factory(exports);
  } else {
    root.peerplaysio = root.peerplaysio || {};
    factory((root.peerplaysio.sjcl = {}));
  }
})(this, function(exports) {

  // INSERT

});
